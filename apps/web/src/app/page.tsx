export default function HomePage() {
  return (
    <main className="mx-auto max-w-2xl p-8">
      <h1 className="text-3xl font-bold">Sensitiv</h1>
      <p className="mt-3 text-sm text-gray-600">
        Location + requirements multi-source research assistant. Scaffold placeholder — the
        bilingual form and run UI land in a later section.
      </p>
      <p className="mt-6 text-xs text-gray-500">
        This is research assistance, not medical, legal, or housing advice.
      </p>
    </main>
  );
}
